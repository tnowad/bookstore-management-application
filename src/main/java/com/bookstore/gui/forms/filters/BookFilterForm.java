package com.bookstore.gui.forms.filters;

import javax.swing.JPanel;

public class BookFilterForm extends JPanel {

  // show list checkbox (show all, show available, show unavailable) (default: show all)
  // show list by (title, author, genre, publisher, isbn, year, price) (default: title)
  // show list by (ascending, descending) (default: ascending)
  // show list by (price, year) (default: price)

  private BookFilterForm() {
    initComponents();
  }

  private void initComponents() {}
}
